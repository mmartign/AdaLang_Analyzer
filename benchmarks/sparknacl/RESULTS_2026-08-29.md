# SPARKNaCl comparison results — 2026-08-29

Re-run 2026-08-29 as part of a full ten-corpus refresh ahead of a version
bump; numbers below are from that latest run.

## Environment and scope

- rod-chapman/SPARKnaCl: `49e3bddf092561ce2b74c134a35acff91a2da9a4`
- AdaLang Analyzer: commit `c43415f` (includes the loop-invariant-preservation
  branch-support widening and the `Unclosed_File_Handle` loop-awareness/
  `FP-062`/`FP-063` work landed since the prior `d1815ac` run — see below)
- GNATprove `--mode=prove` on the unmodified project (FSF 16.1.0, Why3
  1.8.2+git, Z3 4.15.4, CVC5 1.3.2, Alt-Ergo 2.6.1).

## Results

| | Value |
| --- | ---: |
| Proof obligations | 9,484 |
| `Proved_Safe` | 3,875 |
| `Definite_Error` | 0 |
| `Unproved` | 4,097 |
| `Unreachable` | 0 |
| `Unsupported` | 1,512 |
| Matched 1:1 pairs vs. GNATprove | 890 |
| Both safe | 56 |
| Both flag a problem | 4 |
| Possible AdaLang unsoundness | 0 |
| AdaLang false positive | 0 |

## Bit-identical to the 2026-08-25 run

Every number above is unchanged from the prior recorded run, including the
`FP-061` fix confirmation (still 0 `Definite_Error` in `Cipher`'s AES256/
AES128 round-key loops). This run's commit range also included
`--verify`'s loop-invariant-preservation branch-support widening
(`5c2a2c0`); it has no observable effect on this corpus's proof-obligation
numbers — the widening applies to a code shape SPARKNaCl doesn't exercise
in a way that changes the comparison. `Unclosed_File_Handle` loop-awareness
and the `FP-062`/`FP-063` closures are rule-check changes with no
proof-obligation surface, so they are expected to be silent here too.

## Bottom line

Zero possible unsoundness, zero false positives — unchanged across two
consecutive runs on this corpus.
