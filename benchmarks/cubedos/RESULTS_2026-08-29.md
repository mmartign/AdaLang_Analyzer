# CubedOS comparison results — 2026-08-29

Re-run 2026-08-29 as part of a full ten-corpus refresh ahead of a version
bump; numbers below are from that latest run.

## Environment and scope

- cubesatlab/cubedos: `c402301000a5a92237e0f7ab106186a48273cf24`
- AdaLang Analyzer: commit `c43415f`
- GNATprove `--mode=prove`, AUnit resolution via a throwaway local crate:
  unchanged from prior runs.
- CubedOS's own `src/cubedos.gpr` still triggers `FP-040` (a `Call_Expr.
  P_Kind` resolution gap for a cross-project call) on 21 of 49 files, which
  caps how many obligations this corpus's `--verify` pass can register at
  all — read the numbers below as "what limited signal is available," not
  full coverage.

## Results

| | Value |
| --- | ---: |
| Proof obligations | 656 |
| `Proved_Safe` | 0 |
| `Definite_Error` | 0 |
| `Unproved` | 8 |
| `Unreachable` | 73 |
| `Unsupported` | 575 |
| Matched 1:1 pairs vs. GNATprove | 7 |
| Possible AdaLang unsoundness | 0 |
| AdaLang false positive | 0 |
| Both AdaLang and GNATprove flag a problem | 7 |

Bit-identical to the 2026-08-25 run in every field.

## Bottom line

Unchanged from every prior run of this corpus: zero possible unsoundness,
zero false positives. Nothing landed since the last run (`--verify`'s
loop-invariant-preservation branch widening, the `Unclosed_File_Handle`
work) affects this corpus's numbers — `FP-040` still caps what obligations
are registered at all, and this corpus has no `'Succ`/`'Pred` loop-variant
shape.
