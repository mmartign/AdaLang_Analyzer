# Ada_Drivers_Library comparison results — 2026-08-25

Re-run 2026-08-29 as part of a full ten-corpus refresh ahead of a version
bump; numbers below are from that latest run.

## Environment and scope

- AdaCore/Ada_Drivers_Library: `81c04806d267fc12116a6f746c8e05012cef0484`
- AdaLang Analyzer: commit `c43415f`
- Corpus: `arch/ARM/STM32/drivers/`, no `-P` project (see `README.md` for
  why), 90 files passed directly on the command line.

There is no GNATprove lane for this corpus: its purpose is real-code
validation for the `--automotive`-only concurrency-prohibition checks and a
general sanity check on a code shape (bare-metal, interrupt-driven hardware
drivers) none of this project's other corpora resemble, not a
proof-obligation comparison.

## Results

| Lane | Files | Findings |
| --- | ---: | ---: |
| `--recommended` | 90 | 204 |
| `--spark` | 90 | 443 |
| `--automotive` | 90 | 1,982 |

All three lanes completed with clean stderr (no recoverable-failure
warnings, no skipped nodes). Bit-identical to the prior recorded run in
every lane. The concurrency-prohibition breakdown is unchanged too:
`No_Rendezvous` still correctly fires on all 5 real `entry` declarations;
`No_Tasking`/`No_Select`/`No_Requeue`/`No_Asynchronous_Transfer`/
`Potentially_Blocking_Operation` still correctly find nothing (true
negatives, not un-exercised checks).

## Bottom line

Clean run, no analyzer crashes or unexpected failures. No proof-soundness
signal from this corpus (no GNATprove oracle). No regression since the
prior run.
