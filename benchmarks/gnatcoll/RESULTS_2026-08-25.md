# gnatcoll-core comparison results — 2026-08-25

## Environment and scope

- AdaCore/gnatcoll-core: `9f6ffb394793b0ac098fb1e9b206a659680788b3`
- AdaLang Analyzer: commit `d1815ac`
- GNATprove `--mode=flow` hard-stops during flow analysis on this
  unmodified project (pre-existing corpus boundary, unrelated to AdaLang;
  see `README.md`), so there is no GNATprove oracle signal — results below
  are AdaLang's own numbers only.

## Results

| Lane | Findings |
| --- | ---: |
| `--recommended` | 334 |
| `--spark` | 923 |

| `--verify` | Value |
| --- | ---: |
| Proof obligations | 5,043 |
| `Proved_Safe` | 205 |
| `Definite_Error` | 0 |
| `Unproved` | 258 |
| `Unreachable` | 1,072 |
| `Unsupported` | 3,508 |

`--verify` numbers are bit-identical to the prior snapshot — `FP-061` has
no effect here, consistent with this corpus's general-purpose
utility-library shape (JSON, VFS, string builders, OS/process wrappers)
not leaning on loop-variant idioms. `--recommended`/`--spark` finding
counts have grown since the last snapshot as new checks were added —
expected drift for a no-oracle corpus, not a regression signal.

## Bottom line

No regression. No GNATprove oracle available for this corpus (pre-existing
flow-analysis boundary).
