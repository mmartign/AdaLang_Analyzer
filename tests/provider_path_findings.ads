--  Regression fixture for semantic lookup outside the process working
--  directory.  The body needs this spec in order for name/type resolution to
--  succeed.
package Provider_Path_Findings is
   procedure Run;
end Provider_Path_Findings;
